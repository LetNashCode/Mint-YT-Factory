google-genai>=1.30.0
requests>=2.31.0
urllib3>=2.2.0
numpy>=1.26.0

moviepy==1.0.3
Pillow<10
PyYAML>=6.0

pytesseract>=0.3.13

google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.130.0

# Local keyless TTS — Kokoro-82M primary; Edge remains fallback.
kokoro>=0.9.4
soundfile>=0.12.1
edge-tts>=7.0.0

aopenai-whisper>=20250625
torch>=2.2.0

ffmpeg-python>=0.2.0

# Install the project in editable mode so Python loads sitecustomize.py
# during interpreter startup and applies the production quality layer.
-e .
